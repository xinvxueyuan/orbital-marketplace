# ── Orbital 后端环境变量示例 ──────────────────────────────────
# 复制为 .env 并按需修改：cp .env.example .env

# SQLite 数据库文件路径（默认 backend/orbital.db）
DATABASE_URL=sqlite:///backend/orbital.db

# 允许的前端来源（CORS），用逗号分隔
CORS_ORIGINS=http://localhost:5173,http://localhost:4173

# 单用户演示用的固定 userId
DEMO_USER_ID=demo
